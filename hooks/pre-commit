#!/bin/sh
# Pre-commit hook: run tests and generate. Commit aborts if either fails.
set -e
cd "$(git rev-parse --show-toplevel)"
echo "Running tests..."
python3 -m pytest test_generate.py -q
echo "Running generate.py..."
python3 generate.py
echo "Pre-commit checks passed."
